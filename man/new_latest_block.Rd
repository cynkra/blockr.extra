% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latest-block.R
\name{new_latest_block}
\alias{new_latest_block}
\title{Latest-Wins Merge Block}
\usage{
new_latest_block(last_active = NULL, ...)
}
\arguments{
\item{last_active}{Character(1). Name of the input to treat as active on
first load. Usually left \code{NULL} (falls back to the first input) — set
automatically by the serialization layer when restoring a session.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A variadic transform block of class \code{latest_block}.
}
\description{
Variadic transform block that forwards the value of whichever input most
recently changed. Implements the FRP \code{merge} / \code{switchLatest} operator:
one-of semantics across \code{n} inputs, where "active" = "last to fire after
mount".
}
\details{
Typical use: bridge multiple drill-down charts into a single downstream
patient-profile / detail block. Each chart emits a filtered data frame on
click; \code{new_latest_block()} picks the one whose click was most recent and
passes it through unchanged.
}
\section{Reproducibility note}{

The block's output depends on the order of user interactions during the
session, so the board as a static artefact is not fully deterministic.
The most recently active input name is persisted via the block's \code{state}
(\code{last_active}) so restored sessions pick up where they left off.
}

\section{Initial state}{

Before any input fires, the block emits the first input's value (by name
order within \code{...args}). \code{ignoreInit = TRUE} on the per-input observers
prevents the topo-sort mount order from being interpreted as a "click".
}

\examples{
if (interactive()) {
  library(blockr.core)
  serve(
    new_board(
      blocks = list(
        a = new_dataset_block(dataset = "iris"),
        b = new_dataset_block(dataset = "mtcars"),
        pick = new_latest_block()
      ),
      links = links(
        from = c("a", "b"), to = c("pick", "pick"),
        input = c("1", "2")
      )
    )
  )
}

}
