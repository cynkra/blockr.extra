% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result-output.R
\name{block_result_output}
\alias{block_result_output}
\alias{block_result_output.default}
\title{Render a block result, dispatching on the result's class}
\usage{
block_result_output(result, block, session, ...)

\method{block_result_output}{default}(result, block, session, ...)
}
\arguments{
\item{result}{The block's evaluated result.}

\item{block}{The block object.}

\item{session}{The block's Shiny module session.}

\item{...}{Passed to methods.}
}
\value{
A shiny render function, as \code{\link[blockr.core:block_ui]{blockr.core::block_output()}} requires.
}
\description{
\code{block_output()} dispatches on the BLOCK, which is the right hinge for a
block that always draws the same kind of thing. A function or code block
draws whatever its code returned, so the thing worth dispatching on is the
RESULT: a data frame, a gt table, a ggplot, a composer table.
}
\details{
The default method is \code{\link[=render_dynamic_output]{render_dynamic_output()}}, the type switch this
package has always used, so adding this generic changes nothing on its
own. It exists so another package can teach a function block to draw its
own object without that object's renderer having to live here -- and,
unlike an \code{htmltools::as.tags()} method, a method here receives the block's
module \code{session}. That is what a renderer needs to namespace an element id,
register a \code{downloadHandler} on \code{session$output}, or observe a click on
\code{session$input}.

The known case is composer: \code{blockr.sandbox} registers a \code{composed_table}
method that draws the table through blockr.viz's renderer rather than
composer's gt.
}
\section{Called on every re-evaluation}{

blockr.core re-runs \code{block_output()} whenever the block's result changes
(\code{output_render_observer()}), so a method that creates observers must guard
them (once per session, keyed on the namespace). Assigning to
\code{session$output} is idempotent and needs no guard.
}

