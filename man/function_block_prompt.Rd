% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function-block-prompt.R
\name{function_block_prompt}
\alias{function_block_prompt}
\title{Base authoring prompt for function blocks}
\usage{
function_block_prompt()
}
\value{
A single character string.
}
\description{
Returns the canonical guidance the assistant uses to write the \code{fn} of a
\code{\link[=new_function_block]{new_function_block()}}: the \verb{function(data, ...)} contract and the rule that
formal-argument defaults become UI controls (\code{list()} -> multi-select,
\code{c()} -> single-select, single scalars -> numeric/checkbox/text).
}
\details{
Authored once here and reused: the registry entry for \code{new_function_block}
uses it directly, and downstream packages that specialise the function block
(e.g. the composer function block in \pkg{blockr.sandbox}) prepend it to their
own block-specific guidance so the base contract is never duplicated.
}
