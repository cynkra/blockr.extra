% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctrl-filter-block.R
\name{new_ctrl_filter_block}
\alias{new_ctrl_filter_block}
\title{Cohort filter sender block}
\usage{
new_ctrl_filter_block(
  target = "",
  table = "",
  columns = character(),
  label = NULL,
  ...
)
}
\arguments{
\item{target}{Character(1). Block id of the value filter block to control,
e.g. \code{"cohort_filter"}. Empty means "not configured": pass through, send
nothing.}

\item{table}{Character(1). Name of the table in the target's \code{dm} that the
conditions apply to, e.g. \code{"adsl"}.}

\item{columns}{Character vector of source columns a click may claim, for
chart / tile upstreams whose drilled output carries no ARD identity. Empty
(the default) reads the ARD identity instead -- the table / summary case.}

\item{label}{Character(1) or \code{NULL}. Name of the target as shown on the
receipt. Defaults to \code{target}.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\description{
Reads the drilled output of an upstream table, chart or tile block and
pushes the drilled-to condition into a \strong{value filter block elsewhere on the
board}, over the board's external-control channel (\code{\link[=ctrl_send]{ctrl_send()}}). Click a
level row in a summary and the filter narrows to it; re-click to clear.
}
\details{
This is the packaged form of the sender that was prototyped as a
\code{\link[=new_function_block]{new_function_block()}} calling \code{\link[=ctrl_send]{ctrl_send()}} by hand. Prefer this block when
you need more than one or two senders: the claim logic
(\code{\link[=drill_claim_columns]{drill_claim_columns()}}) lives in the package rather than in a deparsed
function string copied once per sender, the configuration is real block
arguments (so it is AI-configurable and restores with the board), and there
is no code editor to mount.
}
\section{Output vs receipt}{

The block's \strong{output} is its input, untouched -- the drilled subset passes
straight through, so the block can still be linked downstream. The
\strong{receipt} ("Sent to Cohort: SEX = F") renders in the block's control
section via \code{\link[=ctrl_receipt]{ctrl_receipt()}}.

The receipt is built from the conditions this block just sent, and nothing
else: the sender never reads the target block back. There is no back-edge,
by design (see \code{\link[=ctrl_receipt]{ctrl_receipt()}}). It is therefore written in the past tense
-- it reports what this block pushed, which stays true even after another
sender replaces the claim on the same target (\code{\link[=ctrl_send]{ctrl_send()}} is
last-write-wins, and \code{\link[=ctrl_clear]{ctrl_clear()}} only ever clears your own claim).
}

\section{Unconfigured}{

With no \code{target}, the block passes data through and sends nothing. It does
not error.
}

\section{Upstream}{
 table, chart or tile:
A \strong{table / summary} drill carries the ARD identity (\code{.variable},
\verb{.group<k>}), and the block reads the claim off it with no further
configuration -- leave \code{columns} empty.

A \strong{chart or tile} drill does not: the chart coerces its input with
\code{as_plain_df()}, which drops those columns, so its drilled output is a plain
row subset with nothing in it naming the drilled column. For those, set
\code{columns} to the source column(s) a click may claim -- typically the chart's
own \code{drill} column, e.g. \code{columns = "SEX"}. See \code{\link[=drill_claim_columns]{drill_claim_columns()}}.
}

\examples{
if (interactive()) {
  # See blockr.pharma/dev/composer-cohort-drill-two-senders.R for a board
  # wiring two of these into a single value filter.
  new_ctrl_filter_block(target = "cohort_filter", table = "adsl")
}

}
