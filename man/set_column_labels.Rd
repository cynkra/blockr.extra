% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labeler-block.R
\name{set_column_labels}
\alias{set_column_labels}
\title{Set column labels on a data frame}
\usage{
set_column_labels(data, labels = character())
}
\arguments{
\item{data}{A data frame.}

\item{labels}{Named character vector (or named list) mapping column names
to label strings.}
}
\value{
\code{data} with updated label attributes.
}
\description{
Sets \code{attr(col, "label")} for each entry of \code{labels} whose name is a
column of \code{data}. Entries for absent columns are skipped silently —
labels are cosmetic and the upstream data shape may change without the
pipeline having to error. An empty string (or \code{NA}) removes the label.
}
\examples{
labelled <- set_column_labels(mtcars, c(mpg = "Miles per gallon"))
attr(labelled$mpg, "label")

}
